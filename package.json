{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node ./node_modules/nodemon/bin/nodemon.js index.js",
    "build:ui": "cd ../ui && rm -rf build && npm run build && cp -r build ../app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ads-client": "^1.14.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-ws": "^5.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
